# SQL Queries Playground

## Docker containers

Run

```
docker-compose up -d
```

Remove

```
docker-compose down
```

## Credentials

- Server: `localhost` or `db` (for adminer on `localhost:8080`)
- Database: `demo`
- User: `admin`
- Password: `password`